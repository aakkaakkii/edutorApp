spring:
  datasource:
    url: jdbc:postgresql://localhost/editorapp
    username: postgres
    password: postgres
  jpa:
    generate-ddl: false
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
    show-sql: false
    hibernate:
      ddl-auto: validate

jwt:
  token:
    secret: JWTeDiToRaPp
    expired: 32400000
